#include <stdio.h>

void new_function() { // return_type Function_name(parameters)
                        // if function doesn't return anything than "void" is used.
    printf("Hello, World!\n");
}

int main(){
    new_function();
}