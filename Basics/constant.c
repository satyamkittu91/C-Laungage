#include <stdio.h>
#define PI2 6.28318 // Macro
/*In C, a macro is a piece of code that is replaced by its value or 
definition before the actual compilation process. It's a preprocessor directive, meaning 
it's handled by the preprocessor before the compiler sees the code.*/

int main() {
    // constant - fixed value that can't be altered by the program during it's execution
    const float PI = 3.14159;
    
    // Now you can't modify the value of PI cause it's constant
    // Here PI2 and PI are constants

}